/**
 * This is place-holder module to allow for extension of Rx with custom
 * operators and such.
 * @module util.Rx
 */
// Require the extension here. Each extension should modify the core Rx module,
// and, optionally return it.
// require('./foo');

module.exports = require('rx');
